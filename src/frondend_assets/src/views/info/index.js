import React from 'react';

const Info = ({ }) => {

    return (
        // <div align="left" width="100%">
        //     <div align="left" width="50%">
        //     <font face="verdana" size="5" color="black">Based on the reverse Gas model of IC platform, we consider Ddos attack as a headache.    The purpose of this product is to help developers defend against DDos attacks and to limit traffic by sending messages to developers who register for the service when a Ddos attack occurs.</font>
        //     </div>
        // </div>
        <table>
            <tr>
            <font face="verdana" size="5" color="black">Based on the reverse Gas model of IC platform, we consider Ddos attack as a headache.</font>
            </tr>
            <tr>
            <font face="verdana" size="5" color="black">The purpose of this product is to help developers defend against DDos attacks and to limit traffic by sending messages to developers who register for the service when a Ddos attack occurs.</font>
            </tr>
        </table>
    );
};

export default Info;
