import React from 'react';

const Info = ({ }) => {

    return (
        <div align="left">
            <font face="verdana" size="5" color="black">Based on the reverse Gas model of IC platform, we consider Ddos attack as a headache.    The purpose of this product is to help developers defend against DDos attacks and to limit traffic by sending messages to developers who register for the service when a Ddos attack occurs.</font>

        </div>
    );
};

export default Info;
